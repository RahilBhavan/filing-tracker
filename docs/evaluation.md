# Evaluation

Current results are in [V2 delivery and measured results](v2-results.md) and [the real benchmark](real-benchmark.json).

The original fictional benchmark is preserved as [V1 evaluation](v1/evaluation.md), with its recorded [verification output](v1/verification.json). The implementation remains available through `compare --engine v1` and the real benchmark's side-by-side engine runs.

The current `verify.py` regenerates fictional development and challenge results using V2. Their full metrics are in `demo/development/metrics.json` and `demo/challenge/metrics.json`. Do not interpret those author-created regression fixtures as independent real-filing evidence.
